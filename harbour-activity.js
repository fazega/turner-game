import {createFishModels} from './fish-models.js';
import * as T from './three.module.js';
import {material} from './materials.js';
export function addHarbourActivity({scene,characters,details,box,cyl,line,wood,rope,trim,rand,obstacles}){
 const actors=[],vendors=[],fishers=[],iron=material('#3b403b',.5),planks=material('#92714e',.9,'timber'),silver=new T.MeshStandardMaterial({color:'#a6b7b0',metalness:.48,roughness:.34}),fin=new T.MeshStandardMaterial({color:'#607e77',metalness:.15,roughness:.5}),eye=new T.MeshStandardMaterial({color:'#1a201b',roughness:.25});
 function actor(kind,x,z,heading=0,scale=1){const root=characters.create(kind,actors.length%3);root.position.set(x,.32,z);root.rotation.y=heading;root.scale.setScalar(scale);root.userData.crowdActivity=true;root.traverse(o=>{if(o.isMesh)o.castShadow=false});scene.add(root);actors.push(root);return root}
 const fishModel=createFishModels();
 function fish(x,y,z,angle=0,size=1){return fishModel(scene,x,y,z,angle,size)}
 function bubble(text){const c=document.createElement('canvas');c.width=768;c.height=128;const g=c.getContext('2d');g.fillStyle='rgba(31,37,29,.88)';g.beginPath();g.roundRect(6,12,756,99,16);g.fill();g.strokeStyle='#bda16b';g.lineWidth=2;g.stroke();g.fillStyle='#f0dfb5';g.font='29px Georgia';g.textAlign='center';g.fillText(text,384,72);const tex=new T.CanvasTexture(c);tex.colorSpace=T.SRGBColorSpace;return tex}
 // Two busy fish stalls leave the central promenade clear.
 for(const [index,x,z]of [[0,-4,17],[1,-38,16]]){box(x,1.05,z,3.2,.12,1.25,planks);for(const dx of [-1.3,1.3])box(x+dx,.68,z,.10,.70,1.1,wood);for(const dz of [-.58,.58])box(x,1.2,z+dz,3.2,.22,.055,planks);for(const dx of [-1.58,0,1.58])box(x+dx,1.2,z,.05,.22,1.15,planks);for(let i=0;i<14;i++)fish(x-1.15+(i%4)*.68,1.16,z-.34+Math.floor(i/4)*.22,(rand()-.5)*.3,.72+rand()*.22);
  // Raised end-grain cutting board, forged knife and a shallow salt bowl.
  box(x+1.16,1.13,z+.22,.58,.045,.48,wood);
  const blade=box(x+1.14,1.164,z+.22,.24,.008,.055,silver);blade.rotation.y=.4;
  const handle=box(x+.97,1.164,z+.15,.15,.026,.045,wood);handle.rotation.y=.4;
  for(let nail=0;nail<2;nail++){const pin=new T.Mesh(new T.SphereGeometry(.008,8,6),iron);pin.position.set(x+.94+nail*.05,1.18,z+.15);scene.add(pin)}
  const bowl=new T.Mesh(new T.LatheGeometry([new T.Vector2(.02,0),new T.Vector2(.09,.01),new T.Vector2(.14,.085),new T.Vector2(.145,.10),new T.Vector2(.125,.10),new T.Vector2(.085,.025),new T.Vector2(.02,.02)],24),trim);bowl.position.set(x+1.3,1.14,z-.34);scene.add(bowl);
  for(let j=0;j<8;j++){const salt=new T.Mesh(new T.IcosahedronGeometry(.012,0),trim);salt.position.set(x+1.3+(rand()-.5)*.14,1.18,z-.34+(rand()-.5)*.14);scene.add(salt)}
  details.barrel(x+2.1,z,scene,.32,.65);details.crate(x-2,z+.5,.32,.6);details.sack(x-1.8,z+1.5);for(let j=0;j<3;j++)details.ring(x+2,.4+j*.035,z+1,.27-j*.04,.025,rope);
  const seller=actor(index?'woman':'man',x,z-1.15,Math.PI),customer=actor(index?'man':'woman',x+.75,z+1.8,.1);customer.userData.speaking=true;
  const sayings=index?['Silver bream! Straight from the boats!','A fine supper for two copper!']:['Fresh sardines! The morning catch!','Come closer—see how they shine!'];const maps=sayings.map(bubble),sprite=new T.Sprite(new T.SpriteMaterial({map:maps[0],transparent:true,depthWrite:false}));sprite.position.set(0,2.35,0);sprite.scale.set(4.5,.75,1);seller.add(sprite);vendors.push({root:seller,sprite,maps,phase:index*7});obstacles.push({x,z,w:1.8,d:.85});
 }
 // Children chase one another on a clear loop away from the cargo and quay edge.
 for(let i=0;i<3;i++){const child=actor(i===1?'girl':'boy',-19,35,0,.66+i*.035);child.name='Child playing chase';child.userData.walkRoute={cx:-19,cz:35,rx:6.5,rz:2.1,speed:1.9+i*.08,angle:i*2.1,run:true}}
 // People stroll along raised paths behind the railings; their height follows the terrain.
 const ground=(x,z)=>.20+Math.max(0,z-50)*.055+Math.sin(x*.022)*Math.sin(Math.max(0,z-50)*.015)+.08;
 for(const x of [-24,24]){for(let z=59;z<113;z+=2){const tile=box(x,ground(x,z)-.025,z,3.8,.08,2.04,material('#b0a58f',1,'stone'));tile.rotation.x=-.055}for(let i=0;i<3;i++){const walker=actor(i%2?'woman':'man',x,65);walker.name='Garden pedestrian';walker.userData.walkRoute={cx:x,cz:85,rx:1.0,rz:23,speed:.66+i*.13,angle:i*2.1+(x>0?1:0),ground}}}
 // Two carpenters kneel over boat timbers near the western shipwright.
 for(const [x,z]of [[-62,19],[-49,21]]){const worker=actor('man',x,z,.65);worker.name='Dock carpenter';worker.userData.activity='Fixing_Kneeling';for(let j=0;j<3;j++)box(x+.4,.39+j*.07,z-.7,1.65,.055,.18,planks);const hand=worker.getObjectByName('hand_r');const hammer=new T.Group();hand.add(hammer);const handle=new T.Mesh(new T.CylinderGeometry(.016,.016,.25,7),wood);handle.position.y=.13;hammer.add(handle);const head=new T.Mesh(new T.BoxGeometry(.13,.055,.055),iron);head.position.y=.26;hammer.add(head)}
 // Anglers hold rods at the water's edge; line and float follow the animated hand.
 for(const x of [-72,-23]){const root=actor('man',x,12.3);root.name='Quayside angler';root.userData.activity='Idle_Torch_Loop';const rod=new T.Mesh(new T.CylinderGeometry(.009,.023,1,8),wood);rod.userData.keepDynamic=true;scene.add(rod);const geo=new T.BufferGeometry().setFromPoints([new T.Vector3(),new T.Vector3(),new T.Vector3()]),thread=new T.Line(geo,new T.LineBasicMaterial({color:'#b8b093',transparent:true,opacity:.7}));thread.userData.keepDynamic=true;scene.add(thread);const float=new T.Mesh(new T.SphereGeometry(.045,8,5),material('#b85a3a'));float.userData.keepDynamic=true;scene.add(float);fishers.push({root,rod,thread,float,x});details.barrel(x-1.0,13,scene,.32,.45);for(let i=0;i<3;i++)fish(x-1+(rand()-.5)*.2,.88,13+(rand()-.5)*.2,rand(),.5)}
 // Stacked wicker creels, oars and drying sailcloth make the working quay less empty.
 for(const x of [-76,-53,18]){for(let i=0;i<3;i++){const z=22+i*.7;for(let y=.4;y<.95;y+=.09)details.ring(x,y,z,.32,.018,rope);for(let j=0;j<10;j++){const a=j*Math.PI/5;line([x+Math.cos(a)*.31,.37,z+Math.sin(a)*.31],[x+Math.cos(a)*.31,.96,z+Math.sin(a)*.31],.013,wood)}}for(let i=0;i<3;i++)line([x+1+i*.15,.4,22],[x+1.5+i*.15,1.8,24],.035,wood)}
 function update(time,camera){for(const v of vendors){const t=(time+v.phase)%16,close=v.root.position.distanceToSquared(camera.position)<28*28;v.root.userData.speaking=t<5;v.sprite.visible=close&&t<4.6;if(v.sprite.visible){v.sprite.material.map=v.maps[Math.floor((time+v.phase)/16)%v.maps.length];v.sprite.material.opacity=Math.min(1,t*3,(4.6-t)*3)}}for(const f of fishers){const hand=f.root.getObjectByName('hand_l'),start=hand.getWorldPosition(new T.Vector3()),tip=new T.Vector3(f.x+.2,3.0+Math.sin(time*.7)*.03,8),delta=tip.clone().sub(start);f.rod.position.copy(start).add(tip).multiplyScalar(.5);f.rod.scale.y=delta.length();f.rod.quaternion.setFromUnitVectors(new T.Vector3(0,1,0),delta.normalize());f.float.position.set(f.x+.3+Math.sin(time*.6)*.05,.04+Math.sin(time*1.2)*.025,5.5);const p=f.thread.geometry.attributes.position,mid=tip.clone().lerp(f.float.position,.5);mid.y-=.35;[tip,mid,f.float.position].forEach((v,i)=>p.setXYZ(i,v.x,v.y,v.z));p.needsUpdate=true;f.thread.geometry.computeBoundingSphere()}}
 return {update,actors,vendors,fishers,children:3,gardenPedestrians:6};
}
